# บันทึก Prompt ที่ใช้ร่วมกับ AI

ตามข้อกำหนดของงาน "กรณีทำงานร่วมกับ AI ให้เก็บ Prompt ที่ป้อนด้วย"

ระหว่างพัฒนามีการป้อนคำสั่งหลายสิบครั้ง ส่วนใหญ่เป็นการสั่งแก้เล็ก ๆ น้อย ๆ
ไฟล์นี้จึงเก็บเฉพาะ **4 คำสั่งหลักที่เปลี่ยนทิศทางของงานจริง ๆ**
พร้อมสิ่งที่ตรวจสอบเองหลังจาก AI ทำงานเสร็จ

เครื่องมือที่ใช้: Claude (Claude Code)

---

## Prompt ที่ 1 — ตั้งโจทย์

> ทำแอป Flutter ตามโจทย์ My Digital Directory บันทึกรายชื่อเพื่อน (ชื่อ, เบอร์, Email)
> ลง SQLite เพิ่มและลบข้อมูลได้ ข้อมูลไม่หายเมื่อปิดแอป
> ต้องมีหน้าฟอร์มกรอกข้อมูลและหน้าแสดงรายการ

**ได้อะไรมา** โครงแอปที่ทำงานได้ครบตามโจทย์ แต่รวมทุกอย่างไว้ในไฟล์เดียว

**ตรวจและแก้เอง** สั่งแยกไฟล์ใหม่เป็น models / data / screens / widgets / theme
และกำหนดว่าหน้าจอห้ามเขียนคำสั่ง SQL เอง ต้องเรียกผ่านชั้นกลาง (repository) เท่านั้น
เพราะไฟล์เดียวยาวเกินไปจนกลับมาแก้ทีหลังไม่ไหว

**ตรวจเรื่องความปลอดภัย** เปิดดูคำสั่ง SQL ทุกจุดว่าค่าที่ผู้ใช้พิมพ์ถูกผูกด้วยเครื่องหมาย `?`
ไม่ได้เอามาต่อเป็นข้อความเข้าไปใน SQL ตรง ๆ ซึ่งเป็นช่องโหว่ SQL injection

---

## Prompt ที่ 2 — เพิ่มคอลัมน์โดยข้อมูลเดิมต้องไม่หาย

> ฐานข้อมูลเดิมมี 4 คอลัมน์ ตอนนี้ต้องเพิ่มเป็น 7 คอลัมน์
> ทำยังไงให้ข้อมูลที่ผู้ใช้บันทึกไว้ตอนเวอร์ชันเก่าไม่หาย

**ได้อะไรมา** วิธีอัปเกรดฐานข้อมูลแบบเพิ่มคอลัมน์เข้าไปในตารางเดิม
แทนการลบตารางทิ้งแล้วสร้างใหม่ (ซึ่งข้อมูลเก่าจะหายหมด)

**ทำไมถึงสำคัญ** คำสั่งนี้กลายเป็นหลักที่ใช้ต่อมาทุกครั้งที่เพิ่มความสามารถใหม่
สุดท้ายฐานข้อมูลถูกอัปเกรดมาถึง 6 รอบ โดยที่รายชื่อที่บันทึกไว้ตั้งแต่รอบแรกยังอยู่ครบ

**ทดสอบเอง** เปิดแอปเวอร์ชันเก่า บันทึกรายชื่อไว้ แล้วอัปเกรดเป็นเวอร์ชันใหม่
กลับมาดูว่ารายชื่อเดิมยังอยู่ครบและอ่านได้ปกติ

---

## Prompt ที่ 3 — กำหนดขอบเขตของความสามารถใหม่

> เพิ่มฟีเจอร์ให้มันไม่เหมือนใคร มีลูกเล่นมากกว่านี้
> ทำได้มากกว่าการจดชื่อ แต่ยังต้องอยู่ในกรอบของสมุดรายชื่อ

**ได้อะไรมา** กลุ่มผู้ติดต่อที่สร้างเองได้ วันเกิดพร้อมนับถอยหลัง บันทึกย่อที่ค้นหาเจอ
รูปโปรไฟล์ หน้าแดชบอร์ดสรุปภาพรวม และปุ่มเลิกทำหลังลบ

**บทเรียน** ประโยคหลัง ("แต่ยังต้องอยู่ในกรอบของสมุดรายชื่อ") สำคัญพอ ๆ กับประโยคแรก
ตอนลองสั่งโดยไม่ใส่ขอบเขต จะได้ฟีเจอร์ที่หลุดจากงานเดิมไปเลย
การบอกขอบเขตไปพร้อมกับสิ่งที่ต้องการ ทำให้ได้ของที่ต่อยอดจากงานเดิมจริง ๆ

---

## Prompt ที่ 4 — ให้ตรวจทั้งระบบ ไม่ใช่แก้เฉพาะจุดที่เห็น

> ทำไมปุ่มล้างข้อมูลในหน้าตั้งค่าถึงกดแล้วไม่ทำงาน
> ไม่แน่ใจว่ามีฟีเจอร์อื่นที่ไม่ทำงานอีกไหม

**สาเหตุจริงที่เจอ** ปุ่มทำงานถูกต้องอยู่แล้ว ข้อมูลถูกลบจริง
แต่หน้ารายชื่อไม่รู้ว่าข้อมูลถูกลบไปแล้ว จึงยังแสดงรายการเก่าค้างไว้
ถ้าเชื่ออาการที่เห็นแล้วสั่งแก้ที่ปุ่ม จะแก้ผิดจุดทั้งหมด

**เจอเพิ่มจากประโยคที่สอง** รูปโปรไฟล์ที่ถ่ายแนวตั้งถูกย่อเป็นสี่เหลี่ยมจัตุรัสจนภาพยืดผิดสัดส่วน
เป็นบั๊กที่ยังไม่มีใครสังเกตเห็น ถ้าไม่ได้ถามเผื่อไว้ก็คงหลุดไปถึงวันส่งงาน

---

## สรุปบทเรียน

1. **สั่งทีละขั้นดีกว่าสั่งรวดเดียว** ขอทั้งแอปในคำสั่งเดียวจะได้โค้ดยาวที่ตรวจสอบไม่ไหว

2. **ต้องตรวจผลลัพธ์เองทุกครั้ง** โดยเฉพาะเรื่องที่พังแล้วกู้ไม่ได้
   เช่น การอัปเกรดฐานข้อมูลต้องไม่ลบข้อมูลเดิม และ SQL ต้องผูกค่าด้วย `?`

3. **บอกขอบเขตไปพร้อมกับสิ่งที่ต้องการ** "มีลูกเล่นมากขึ้นแต่ยังอยู่ในกรอบสมุดรายชื่อ"
   ได้ผลตรงกว่าการบอกแค่ว่า "ทำให้เจ๋งขึ้น"

4. **กล้าสั่งแก้เมื่อ AI เลือกทางที่ง่ายกว่าให้เอง** ตอนสั่งทำรูปโปรไฟล์
   AI เสนอให้ใช้อีโมจิพร้อมเหตุผลที่ฟังดูดี แต่ไม่ตรงกับที่ต้องการ
   พอยืนยันให้ทำแบบเลือกรูปจากเครื่องจริง ๆ สุดท้ายได้ทั้งสองทาง

5. **อาการที่เห็นกับสาเหตุจริงมักอยู่คนละที่** และการถามเผื่อว่า "มีอะไรพังอีกไหม"
   มักเจอปัญหาที่ยังไม่มีใครสังเกตเห็น

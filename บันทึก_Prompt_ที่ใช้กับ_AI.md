# บันทึก Prompt ที่ใช้ร่วมกับ AI

ตามข้อกำหนดของงาน "กรณีทำงานร่วมกับ AI ให้เก็บ Prompt ที่ป้อนด้วย"
ไฟล์นี้บันทึกคำสั่งที่ป้อนให้ AI ตามลำดับการทำงานจริง พร้อมผลที่ได้และการตัดสินใจของผู้พัฒนา

เครื่องมือที่ใช้: Claude (Claude Code)

---

## ขั้นที่ 1 — กำหนดโจทย์และโครงสร้างเริ่มต้น

**Prompt**
> ทำแอป Flutter ตามโจทย์ My Digital Directory บันทึกรายชื่อเพื่อน (ชื่อ, เบอร์, Email)
> ลง SQLite เพิ่มและลบข้อมูลได้ ข้อมูลไม่หายเมื่อปิดแอป
> ต้องมีหน้าฟอร์มกรอกข้อมูลและหน้าแสดงรายการ

**ผลที่ได้** โครงแอปเบื้องต้น หน้าเดียว มีฟอร์มและรายการ

**สิ่งที่ผู้พัฒนาปรับเอง** แยกไฟล์ออกเป็นโฟลเดอร์ ไม่เก็บทุกอย่างไว้ใน `main.dart`
เพราะไฟล์เดียวยาวเกินไปจนแก้ยาก

---

## ขั้นที่ 2 — ออกแบบหน้าจอ

**Prompt**
> สรุปแนวทางจากงานออกแบบจริง: พื้นหลัง navy ไม่ใช่ดำสนิท (#0F172A),
> ไล่ระดับผิวการ์ด, bottom navigation, ปุ่มห่างกัน 8-12 pt, contrast 4.5:1
> เริ่มเขียนโครงใหม่

**ผลที่ได้** ไฟล์ `theme/app_theme.dart` ที่รวมสีและระยะห่างไว้ที่เดียว

**เหตุผลของการเลือก** พื้นหลังดำสนิทคู่กับตัวอักษรสีขาวให้ค่า contrast สูงเกินไป
มองนาน ๆ แล้วล้าตา สีน้ำเงินเข้มลดความแรงลงโดยยังอ่านง่าย

---

## ขั้นที่ 3 — แยกโครงสร้างไฟล์

**Prompt**
> แยกโค้ดเป็นโฟลเดอร์ models / data / screens / widgets / theme
> ให้หน้าจอไม่ต้องเขียน SQL เอง เรียกผ่าน repository อย่างเดียว

**ผลที่ได้** `ContactRepository` เป็นชั้นกลางระหว่างหน้าจอกับฐานข้อมูล

---

## ขั้นที่ 4 — เพิ่มความสามารถ

**Prompt**
> เพิ่มค้นหา (ค้นทั้งชื่อ เบอร์ อีเมล), แก้ไขโดยแตะที่การ์ด, รายการโปรดติดดาว,
> สลับเรียงใหม่สุด/ตามชื่อ, ยืนยันก่อนลบ

**ผลที่ได้** ครบทุกข้อ ค้นหาใช้ SQL `LIKE` ค้นสามคอลัมน์ในคำสั่งเดียว

**สิ่งที่ตรวจเพิ่ม** ยืนยันว่าค่าที่ผู้ใช้พิมพ์ถูกผูกด้วย `?` ไม่ใช่ต่อสตริงเข้า SQL ตรง ๆ
เพื่อกัน SQL injection

---

## ขั้นที่ 5 — อัปเกรดฐานข้อมูลโดยข้อมูลเดิมไม่หาย

**Prompt**
> ฐานข้อมูลเดิมมี 4 คอลัมน์ ตอนนี้ต้องเพิ่มเป็น 7 คอลัมน์
> ทำยังไงให้ข้อมูลที่ผู้ใช้บันทึกไว้ตอนเวอร์ชันเก่าไม่หาย

**ผลที่ได้** ใช้ `onUpgrade` คู่กับ `ALTER TABLE` เพิ่มคอลัมน์ทีละตัว
แทนการ `DROP TABLE` แล้วสร้างใหม่

**การทดสอบ** เปิดแอปเวอร์ชันเก่า บันทึกรายชื่อไว้ แล้วอัปเกรดเป็นเวอร์ชันใหม่
รายชื่อเดิมยังอยู่ครบ

---

## ขั้นที่ 6 — หน้าแดชบอร์ด

**Prompt**
> เพิ่มแท็บที่สอง แสดงการ์ดตัวเลข 3 ใบ กราฟแท่ง 7 วันย้อนหลัง
> อักษรที่พบบ่อย และ 3 รายชื่อล่าสุด

**ปัญหาที่เจอ** กราฟแท่งล้นกรอบ (overflow)

**Prompt แก้ปัญหา**
> กราฟแท่งขึ้น overflow ตอนจอเตี้ย แก้ให้ไม่ล้น

**วิธีแก้** กำหนดความสูงพื้นที่กราฟตายตัว แล้วคำนวณความสูงแท่ง
จากพื้นที่ที่เหลือหลังหักที่ของตัวเลขและป้ายชื่อวันออกแล้ว

---

## ขั้นที่ 7 — ตรวจสอบและเอกสาร

**Prompt**
> รัน flutter analyze ให้ผ่านสะอาด แล้วเขียน README อธิบายทีละไฟล์ว่าอันไหนทำอะไร
> พร้อมคู่มือการใช้งานสำหรับผู้ใช้ทั่วไป

**ผลที่ได้** `README.md` สำหรับผู้อ่านโค้ด และ `คู่มือการใช้งาน.md` สำหรับผู้ใช้

---

## ขั้นที่ 8 — แก้ปัญหาเปิดเว็บแล้วเป็นหน้าขาว

**Prompt**
> เว็บยังพังอยู่ ช่วยสแกนแล้วแก้ให้หน่อย เปิดไม่ได้

**สิ่งที่ AI ตรวจพบ** โค้ดไม่ผิด `flutter analyze` ผ่าน และ `flutter build web` สำเร็จ
ปัญหาอยู่ที่วิธีรัน เพราะโหมดพัฒนาต้องโหลดสคริปต์กว่า 500 ไฟล์ผ่านตัวกลางของ Codespaces
ถ้าจังหวะไหนหลุดจะได้หน้าขาวโดยไม่มีข้อความบอก

**ผลที่ได้** สคริปต์ `serve_web.sh` ที่ build เป็นไฟล์เดียวแล้วเสิร์ฟแบบ static
พร้อมตั้งชนิดไฟล์ของ `.wasm` ให้ถูกต้อง

**บทเรียน** อาการ "แอปพัง" ไม่ได้แปลว่าโค้ดผิดเสมอไป ต้องแยกให้ออกระหว่าง
ปัญหาของโค้ดกับปัญหาของสภาพแวดล้อมที่รัน

---

## ขั้นที่ 9 — แก้ปัญหาเปิดฐานข้อมูลบนเว็บไม่สำเร็จ

**Prompt**
> (วางข้อความผิดพลาดจากคอนโซลของเบราว์เซอร์ทั้งก้อน)
> เชื่อมฐานข้อมูลไม่ได้

**สิ่งที่ AI ตรวจพบ** ไฟล์ `sqlite3.wasm` เป็นคนละเวอร์ชันกับแพ็กเกจที่โปรเจกต์ใช้จริง
เพราะสคริปต์ติดตั้งดาวน์โหลดตามเวอร์ชันขั้นต่ำที่แพ็กเกจระบุ (2.4.6)
แต่ตัวจัดการแพ็กเกจเลือกเวอร์ชันใหม่กว่าให้ (3.5.2)

**ผลที่ได้** สคริปต์ `setup_web.sh` ที่อ่านเวอร์ชันจริงจาก `pubspec.lock`
แล้วดึงไฟล์ให้ตรงกัน และเพิ่มการดักจับข้อผิดพลาดในแอปให้แสดงสาเหตุบนหน้าจอ
แทนที่จะค้างอยู่ที่วงกลมโหลดตลอดไป

**บทเรียน** ต้องบังคับให้แอปบอกสาเหตุเสมอเมื่อทำงานไม่สำเร็จ
หน้าจอที่หมุนค้างโดยไม่บอกอะไรเลยทำให้หาสาเหตุไม่ได้ ทั้งของผู้ใช้และของคนเขียนเอง

---

## ขั้นที่ 10 — ปรับหน้าแดชบอร์ดและทบทวนชนิดข้อมูล

**Prompt**
> แก้หน้าแดชบอร์ดหน่อย ไม่เห็นมีฟีเจอร์อะไรเลย
> แล้วทำไมเบอร์โทรถึงเก็บเป็นตัวหนังสือ ไม่ใช่ตัวเลข

**ผลที่ได้** แดชบอร์ดแสดงการ์ดทุกใบเสมอแม้ยังไม่มีข้อมูล (ของเดิมซ่อนทั้งหน้า)
พร้อมแถบสรุป กราฟ 7 วัน และอันดับต่าง ๆ

**คำตอบเรื่องเบอร์โทร** ยืนยันว่าเก็บเป็นข้อความถูกต้องแล้ว เพราะเบอร์โทรไม่ใช่ "จำนวน"
แต่เป็น "รหัส" ถ้าเก็บเป็นตัวเลขจะทำให้เลขศูนย์นำหน้าหายไป และเก็บเครื่องหมาย + หรือขีดไม่ได้
ทั้งที่ไม่เคยเอาเบอร์ไปคำนวณอะไรเลย

**บทเรียน** การตั้งคำถามกับสิ่งที่ดูแปลกเป็นเรื่องดี แต่ต้องฟังเหตุผลให้จบก่อนสั่งแก้
ไม่ใช่ทุกอย่างที่ดูผิดจะผิดจริง

---

## ขั้นที่ 11 — เพิ่มความสามารถให้มากกว่าการจดชื่อ

**Prompt**
> เพิ่มฟีเจอร์ให้มันไม่เหมือนใคร มีลูกเล่นมากกว่านี้
> ทำได้มากกว่าการจดชื่อ แต่ยังต้องอยู่ในกรอบของสมุดรายชื่อ

**ผลที่ได้** ฐานข้อมูลขึ้นเป็นเวอร์ชัน 3 เพิ่มกลุ่มผู้ติดต่อ 5 กลุ่ม
วันเกิดพร้อมนับถอยหลัง และบันทึกย่อที่ค้นหาเจอ
พร้อมเมนูคัดลอกข้อมูล และปุ่มเลิกทำหลังลบ

**บทเรียน** การกำหนดขอบเขตในคำสั่ง ("ยังต้องอยู่ในกรอบของสมุดรายชื่อ")
สำคัญพอ ๆ กับการบอกว่าต้องการอะไร ถ้าไม่กำหนดไว้จะได้ฟีเจอร์ที่หลุดจากงานเดิม

---

## ขั้นที่ 12 — รองรับหลายขนาดจอ และปรับสีสัน

**Prompt**
> ทำให้ responsive กว่านี้ และเปลี่ยนสีให้มีสีสันมากขึ้น
>
> ทำแถบข้างกับการจัดหน้าให้ดีกว่านี้

**ผลที่ได้** จอกว้างเปลี่ยนแถบเมนูล่างเป็นแถบข้างพร้อมป้ายจำนวนรายชื่อ
เนื้อหาจัดเป็นสองคอลัมน์ และคุมความกว้างสูงสุดไม่ให้บรรทัดยาวจนอ่านยาก
พร้อมเพิ่มสีประจำหมวดต่าง ๆ

**บทเรียน** งานที่บอกเป็นความรู้สึก ("สีไม่สวย" "จัดหน้าไม่ดี")
ต้องแปลงเป็นเงื่อนไขที่วัดได้ก่อน เช่น จอกว้างกี่พิกเซลถึงเปลี่ยนรูปแบบ
ไม่งั้นจะแก้ไปเรื่อยโดยไม่รู้ว่าเมื่อไหร่ถึงจะพอ

---

## ขั้นที่ 13 — คู่มือในแอป และตัดข้อมูลที่ซ้ำซ้อน

**Prompt**
> ทำคู่มือวิธีใช้ให้หน่อย และตัดข้อมูลที่ไม่จำเป็นในแดชบอร์ดออก
>
> ไม่ใช่คู่มือแยกข้างนอก ต้องเป็นหน้าในแอปเหมือนหน้ารายชื่อ

**ผลที่ได้** หน้าคู่มือในแอปที่พับเก็บได้ทีละหัวข้อ
และตัดการ์ดตัวเลขในแดชบอร์ดที่แสดงค่าซ้ำกับแถบสรุปออกทั้งแถว
พร้อมลบไฟล์ที่ไม่ได้ใช้แล้วทิ้ง ไม่ทิ้งโค้ดตายไว้

**บทเรียน** การสั่งครั้งแรกกำกวมทำให้ได้ผลลัพธ์ผิดที่ (ได้หน้าเว็บแยกแทนหน้าในแอป)
ต้องระบุให้ชัดว่าของที่ต้องการจะไปอยู่ตรงไหน
และการ "ตัดออก" ก็เป็นการพัฒนาเหมือนกัน ไม่ใช่มีแต่การเพิ่มเข้าไป

---

## ขั้นที่ 14 — หน้าตั้งค่า ช่องทางออนไลน์ และรูปโปรไฟล์

**Prompt**
> เพิ่มหน้าตั้งค่าด้วย ส่วนคู่มือทำเป็นเมนูดึงลงให้ใช้ง่ายกว่านี้
> แล้วให้เพิ่มช่องทางติดต่ออื่นอย่าง IG LINE FB แบบไม่บังคับกรอก
> และให้เปลี่ยนรูปโปรไฟล์รายคนได้

**ผลที่ได้** ฐานข้อมูลขึ้นเป็นเวอร์ชัน 4 เพิ่มช่องทางออนไลน์สามช่อง
และเพิ่มตาราง `settings` แยกต่างหากสำหรับเก็บค่าตั้งค่าแบบคีย์-ค่า
คู่มือกับตั้งค่าย้ายไปอยู่ในเมนูสามจุดมุมขวาบน

**สิ่งที่ผู้พัฒนาสั่งแก้** AI ทำรูปโปรไฟล์เป็นการเลือกอีโมจิมาให้ก่อน
โดยให้เหตุผลว่าไฟล์ฐานข้อมูลจะไม่บวม แต่ไม่ตรงกับที่ต้องการ
จึงสั่งให้เปลี่ยนเป็นเลือกไฟล์รูปจริงจากเครื่อง

**บทเรียน** เมื่อ AI เลือกทางที่ง่ายกว่าให้เอง ต้องกล้าสั่งให้ทำตามที่ต้องการจริง
ผลลัพธ์สุดท้ายได้ทั้งสองทาง คือใส่รูปจริงก็ได้ ใช้อีโมจิก็ได้

---

## ขั้นที่ 15 — เก็บรูปโปรไฟล์โดยไม่ให้ฐานข้อมูลบวม

**Prompt**
> รูปโปรไฟล์ต้องเลือกจากไฟล์ของผู้ใช้ ไม่ใช่อีโมจิ

**ผลที่ได้** ฐานข้อมูลขึ้นเป็นเวอร์ชัน 5 เพิ่มคอลัมน์เก็บไฟล์รูป
โดยย่อรูปเหลือด้านละไม่เกิน 256 จุดก่อนเก็บทุกครั้ง

**เหตุผลที่ต้องย่อ** รูปจากกล้องมือถือใบหนึ่งหนัก 3-5 เมกะไบต์
ถ้าเก็บทั้งใบ แค่ยี่สิบคนไฟล์ฐานข้อมูลก็ร้อยเมกะไบต์แล้ว และช้าทุกครั้งที่เปิดรายการ
ย่อก่อนเก็บเหลือหลักสิบกิโลไบต์ต่อคน ทั้งที่แสดงจริงแค่วงกลมเล็ก ๆ

**บทเรียน** ต้องคิดถึงขนาดข้อมูลตั้งแต่ตอนออกแบบ ไม่ใช่รอให้แอปช้าแล้วค่อยแก้

---

## ขั้นที่ 16 — ภาพเคลื่อนไหว

**Prompt**
> ใส่ภาพเคลื่อนไหวให้ทุกหน้าทุกปุ่มที่กด
>
> ทำให้ดีกว่านี้ เช่นตอนเข้าหน้าแดชบอร์ดให้มีภาพเคลื่อนไหว

**ผลที่ได้** รวมความเร็วและเส้นโค้งของภาพเคลื่อนไหวไว้ที่ไฟล์เดียว
การ์ดไล่โผล่ทีละใบ กราฟวิ่งขึ้นจากศูนย์ ตัวเลขนับขึ้น และหน้าเลื่อนเข้าจากขวา

**ปัญหาที่เจอระหว่างทาง** ครั้งแรกภาพเคลื่อนไหวเล่นแค่ครั้งเดียวตลอดอายุแอป
เพราะโครงสร้างที่ใช้สลับแท็บเก็บหน้าเดิมไว้ ไม่ได้สร้างใหม่
แก้โดยใส่ตัวนับรอบการเข้าแท็บ เพื่อบังคับให้สร้างการ์ดใหม่ทุกครั้ง

**สิ่งที่ตรวจเพิ่มเอง** ทุกจุดต้องเช็กก่อนว่าผู้ใช้ตั้งระบบปิดภาพเคลื่อนไหวไว้หรือไม่
เพราะบางคนเวียนหัวกับภาพที่ขยับ

**บทเรียน** ภาพเคลื่อนไหวต้องมีหน้าที่ ไม่ใช่ใส่เพราะขยับได้
เช่น แท่งกราฟวิ่งจากซ้ายไปขวาเพราะตรงกับทิศทางของเวลาที่กราฟสื่อ

---

## ขั้นที่ 17 — สร้างไฟล์ติดตั้งสำหรับมือถือ

**Prompt**
> ทำเป็นไฟล์ APK ให้หน่อย จะได้โหลดลงมือถือ

**ปัญหาที่เจอ** ไฟล์ที่ได้ครั้งแรกใหญ่ถึง 54 เมกะไบต์
เพราะรวมโค้ดของชิปทุกแบบไว้ในไฟล์เดียว

**วิธีแก้** สั่งแยกไฟล์ตามชนิดชิป เหลือไฟล์ละประมาณ 19 เมกะไบต์
สำหรับมือถือทั่วไป

---

## ขั้นที่ 18 — ตรวจหาฟีเจอร์ที่ไม่ทำงาน

**Prompt**
> ทำไมปุ่มล้างข้อมูลในหน้าตั้งค่าถึงกดแล้วไม่ทำงาน
> ไม่แน่ใจว่ามีฟีเจอร์อื่นที่ไม่ทำงานอีกไหม

**สิ่งที่ตรวจพบ** ปุ่มลบข้อมูลสำเร็จจริง แต่หน้ารายชื่อไม่รู้ว่าข้อมูลถูกลบไปแล้ว
เพราะออกแบบให้โหลดใหม่เฉพาะตอนที่ตัวเองเป็นคนสั่งเท่านั้น
จึงยังแสดงรายการเก่าค้างอยู่ ทำให้ดูเหมือนปุ่มไม่ทำงาน

**บั๊กที่เจอเพิ่มระหว่างตรวจ** การย่อรูปโปรไฟล์สั่งย่อเป็นจัตุรัสตรง ๆ
ทำให้รูปที่ถ่ายแนวตั้งถูกยืดจนผิดสัดส่วน แก้โดยอ่านขนาดจริงมาคำนวณก่อนย่อ

**บทเรียน** อาการที่ผู้ใช้เห็นกับสาเหตุจริงมักอยู่คนละที่กัน
ผู้ใช้เห็นว่า "ปุ่มไม่ทำงาน" แต่สาเหตุจริงอยู่ที่หน้าอื่นที่ไม่รู้ว่าข้อมูลเปลี่ยนไปแล้ว
และการให้ตรวจทั้งระบบเผื่อไว้ ทำให้เจอบั๊กที่ยังไม่มีใครสังเกตเห็นเพิ่มอีกหนึ่งจุด

---

## สรุปบทเรียนจากการทำงานร่วมกับ AI

1. **สั่งทีละขั้นได้ผลดีกว่าสั่งรวดเดียว** — ขอทั้งแอปในคำสั่งเดียวจะได้โค้ดที่ยาว
   และตรวจสอบยาก แยกเป็นขั้นแล้วตรวจทีละขั้นทำให้พบปัญหาได้เร็ว

2. **ต้องตรวจผลลัพธ์เองทุกครั้ง** — เช่น ตรวจว่าใช้ `?` ผูกค่าใน SQL จริงหรือไม่
   และตรวจว่าการอัปเกรดฐานข้อมูลไม่ลบข้อมูลเดิม

3. **บอกเหตุผลเบื้องหลังไปด้วย** — เช่น บอกว่าต้องการค่าความต่างของสี 4.5:1
   ได้ผลตรงกว่าการบอกแค่ว่า "ทำให้สวย"

4. **ระบุให้ชัดว่าของที่ต้องการจะไปอยู่ตรงไหน** — ตอนสั่งทำคู่มือครั้งแรก
   ได้เป็นหน้าเว็บแยกข้างนอก ทั้งที่ต้องการหน้าในแอป เสียเวลาไปหนึ่งรอบ

5. **กล้าสั่งแก้เมื่อ AI เลือกทางที่ง่ายกว่าให้เอง** — เรื่องรูปโปรไฟล์
   AI เสนออีโมจิพร้อมเหตุผลที่ฟังดูดี แต่ไม่ตรงกับที่ต้องการ
   พอสั่งให้ทำตามที่ต้องการจริง สุดท้ายได้ทั้งสองทาง

6. **กำหนดขอบเขตพร้อมกับคำสั่ง** — บอกว่า "ให้มีลูกเล่นมากขึ้น
   แต่ยังต้องอยู่ในกรอบของสมุดรายชื่อ" ทำให้ได้ฟีเจอร์ที่ต่อยอดจากงานเดิม
   ไม่ใช่ฟีเจอร์ที่หลุดออกไปคนละเรื่อง

7. **อาการที่เห็นกับสาเหตุจริงมักอยู่คนละที่** — ปุ่มล้างข้อมูลที่ดูเหมือนเสีย
   จริง ๆ แล้วทำงานถูกต้อง แต่หน้าอื่นไม่รู้ว่าข้อมูลเปลี่ยนไปแล้ว
   ถ้าเชื่ออาการที่เห็นแล้วสั่งแก้ที่ปุ่ม จะแก้ผิดจุด

8. **ให้ตรวจทั้งระบบเผื่อไว้ด้วย** — การถามว่า "มีฟีเจอร์อื่นที่ไม่ทำงานอีกไหม"
   ทำให้เจอบั๊กเรื่องรูปผิดสัดส่วนที่ยังไม่มีใครสังเกตเห็น

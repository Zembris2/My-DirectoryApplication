# วิธีรันและสร้าง APK โดยไม่ต้องติดตั้ง Flutter ในเครื่อง

ใช้ได้กับเครื่องที่ติดตั้งโปรแกรมเพิ่มไม่ได้ ทุกขั้นตอนทำผ่านเว็บเบราว์เซอร์อย่างเดียว

---

## ทางเลือกที่ 1 — GitHub Codespaces (แนะนำ ได้ไฟล์ APK จริง)

Codespaces คือเครื่อง Linux บนคลาวด์ที่เปิดผ่านเบราว์เซอร์ บัญชีฟรีใช้ได้ 60 ชั่วโมงต่อเดือน
โปรเจกต์นี้มีไฟล์ `.devcontainer/devcontainer.json` เตรียมไว้แล้ว
Codespaces จะติดตั้ง Flutter และ Android SDK ให้อัตโนมัติ

### ขั้นตอน

1. เข้า [github.com](https://github.com) แล้วล็อกอิน (สมัครฟรีถ้ายังไม่มี)
2. กด **+** มุมขวาบน เลือก **New repository**
   - ตั้งชื่อ `my_directory`
   - เลือก **Private** ถ้าไม่อยากให้คนอื่นเห็น
   - กด **Create repository**
3. ในหน้า repository กด **uploading an existing file**
4. ลากไฟล์ทั้งหมดจากโฟลเดอร์โปรเจกต์เข้าไป (ลากทั้งโฟลเดอร์ `lib` ได้เลย) แล้วกด **Commit changes**
5. กดปุ่มเขียว **Code** เลือกแท็บ **Codespaces** แล้วกด **Create codespace on main**
6. รอประมาณ 2–4 นาทีให้เครื่องเตรียมตัวเสร็จ จะได้ VS Code เต็มรูปแบบในเบราว์เซอร์

### คำสั่งที่ต้องรันใน Terminal ของ Codespaces

สร้างไฟล์แพลตฟอร์มที่ยังไม่มี (android, web) ครั้งเดียว

```bash
flutter create .
```

ติดตั้ง dependency

```bash
flutter pub get
```

ตรวจคุณภาพโค้ด — ควรได้ผลว่าไม่พบปัญหา

```bash
flutter analyze
```

สร้างไฟล์ APK

```bash
flutter build apk --release
```

ครั้งแรกจะนานประมาณ 5–10 นาที เพราะต้องดาวน์โหลด Gradle
เมื่อเสร็จ ไฟล์จะอยู่ที่ `build/app/outputs/flutter-apk/app-release.apk`

### วิธีดาวน์โหลด APK ออกมา

ในแถบไฟล์ด้านซ้ายของ Codespaces ไล่ไปที่ `build/app/outputs/flutter-apk/`
คลิกขวาที่ `app-release.apk` แล้วเลือก **Download**

---

## ทางเลือกที่ 2 — รันดูหน้าจอบนเว็บทันที

ถ้าต้องการแค่เห็นแอปทำงานเพื่ออัดคลิป ไม่ต้องได้ไฟล์ APK

รันใน Codespaces

```bash
dart run sqflite_common_ffi_web:setup
flutter run -d web-server --web-port 8080 --web-hostname 0.0.0.0 -t lib/main_web.dart
```

Codespaces จะขึ้นแจ้งเตือนให้เปิดพอร์ต 8080 กด **Open in Browser** แล้วใช้งานได้ทันที

บนเว็บ ข้อมูลถูกเก็บใน IndexedDB ของเบราว์เซอร์ผ่าน SQLite เวอร์ชัน WebAssembly
ปิดแท็บแล้วเปิดใหม่ข้อมูลยังอยู่ ใช้สาธิตข้อ "ข้อมูลไม่หายเมื่อปิดแอป" ได้

---

## ทางเลือกที่ 3 — zapp.run (เร็วที่สุด ไม่ต้องสมัครอะไรเลย)

เหมาะกับการเปิดดูโค้ดและหน้าจอเร็ว ๆ แต่ **สร้าง APK ไม่ได้**

1. เข้า [zapp.run](https://zapp.run)
2. กด **New Project** เลือก Flutter
3. ก๊อปไฟล์ในโฟลเดอร์ `lib` เข้าไปทีละไฟล์ตามโครงสร้างเดิม
4. เพิ่ม dependency `sqflite` และ `path` ในช่อง pubspec

ข้อจำกัด: zapp.run รันบนเว็บ อาจไม่รองรับ SQLite เวอร์ชัน WebAssembly
ถ้าติดปัญหาให้ใช้ทางเลือกที่ 1 หรือ 2 แทน

---

## ทางเลือกที่ 4 — Firebase Studio

1. เข้า [idx.google.com](https://idx.google.com) ล็อกอินด้วยบัญชี Google
2. เลือก **Import a repo** แล้วใส่ลิงก์ repository จากขั้นตอนที่ 1
3. Firebase Studio รองรับ Flutter ในตัว รันได้ทั้งเว็บและอีมูเลเตอร์ Android

---

## สรุปว่าควรเลือกอันไหน

| ต้องการ | ใช้ |
| --- | --- |
| ได้ไฟล์ APK ไปติดตั้งบนมือถือจริง | ทางเลือกที่ 1 |
| แค่อัดคลิปสาธิตให้ทัน | ทางเลือกที่ 2 |
| เปิดดูโค้ดเร็ว ๆ ไม่สมัครอะไร | ทางเลือกที่ 3 |

---

## ข้อควรระวังเมื่อใช้เครื่องของสถานประกอบการ

ทุกทางเลือกข้างต้นรันบนคลาวด์ เครื่องที่ใช้เปิดแค่เบราว์เซอร์เท่านั้น
ไม่ต้องติดตั้ง Flutter, Android SDK, Java หรือ Gradle ลงเครื่อง

หลีกเลี่ยงการติดตั้งชุดเครื่องมือพัฒนาลงเครื่องของสถานประกอบการโดยตรง
เพราะโปรแกรมป้องกันภัยระดับองค์กรมักแจ้งเตือนพฤติกรรมเหล่านี้
เช่น การรันไฟล์ที่ไม่ได้เซ็นรับรอง การเปิดพอร์ตของ `adb` และการดาวน์โหลดไฟล์จำนวนมาก
อีกทั้งการติดตั้งซอฟต์แวร์เองมักขัดกับนโยบายการใช้งานเครื่องขององค์กร
